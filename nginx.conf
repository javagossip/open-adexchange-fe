server {
    listen 80;
    server_name localhost;

    # gzip 压缩配置
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/x-javascript application/xml application/json;
    gzip_comp_level 6;

    # 静态资源根目录
    root /usr/share/nginx/html;
    index index.html;

    # 静态资源缓存配置
    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Vue Router history 模式支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理配置（根据实际后端地址修改）
    location /mos/ {
        rewrite ^/mos/(.*)$ /$1 break;
        proxy_pass http://openadx-mos:9090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SSP 广告接口代理（8080端口）
    location /publisher-api/ {
        rewrite ^/publisher-api/(.*)$ /$1 break;
        proxy_pass http://openadx-publisher-api:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Tracking 追踪接口代理（8083端口）
    location /tracking-api/ {
        rewrite ^/tracking-api/(.*)$ /$1 break;
        proxy_pass http://openadx-tracking-api:8083;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # XXL-JOB Admin 代理（任务调度控制台，xxl-job 服务）
    # location = /xxljob-admin {
    #     return 301 /xxljob-admin/;
    # }
    location /xxl-job-admin {
        #rewrite ^/xxljob-admin/(.*)$ /xxl-job-admin/$1 break;
        proxy_pass http://xxl-job:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 健康检查
    location /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

